/**
 * @type {import('gatsby').GatsbyConfig}
 */
module.exports = {
  siteMetadata: {
    title: `Codename 10x returns`,
    siteUrl: `https://www.yourdomain.tld`,
  },
  plugins: [
    //This plugin exists only once but can consume an array of endpoints
  ],
};
